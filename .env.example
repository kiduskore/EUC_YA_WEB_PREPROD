# Server Configuration
PORT=3000
SESSION_SECRET=euc_super_secret_key_2026

# Database (optional - fallback to memory state)
DATABASE_URL=

# Email (optional SMTP config)
SMTP_SERVER=smtp.ionos.com
SMTP_PORT=587
SMTP_USER=info@eucmaryland.org
SMTP_PASS=
